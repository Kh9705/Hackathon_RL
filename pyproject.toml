[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sc-env"
version = "1.0.0"
description = "Supply Chain Environment for OpenEnv"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]
